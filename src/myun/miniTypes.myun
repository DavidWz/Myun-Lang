fib(n::Int)::Int
    if n <= 2 then
        return 1
    else
        return fib(n-2)+fib(n-1)
    end
end

/*sumUpTo(n::Int)::Int
    sum = 0
    for i from 1 to n do
        sum = sum + i
    end
    return sum
end*/

script miniTypes
    if fib(10) is 55 then
        return 0
    else
        return 1
    end
end